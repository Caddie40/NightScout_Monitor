services:
  librelinkup-uploader:
    build: .
    container_name: librelinkup-nightscout-uploader
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - uploader-data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  uploader-data:
